---
last_updated: "2026-04-20T10:00:00"
current_agent: "Claude Code"
---
# Project State
> Khởi tạo bởi hệ thống tự động hoá AI-SSoT

## Current Sprint
Sprint 3: Story Bank Feature

## Active Tasks
- [x] Modal pipeline với Valtec TTS (deployed optimindss3)
- [x] LiveKit agent: Silero VAD + Whisper + Gemini + Valtec
- [x] STT/LLM/TTS quality fixes (double VAD, system prompt, audio normalization)
- [x] Frontend LiveKit room reconnection stability
- [x] Evaluation flow after LiveKit session
- [x] LiveKit agent pre-loading & readiness signaling (Pre-create on confirm page)
- [x] Story Bank P0: Core CRUD + AI STAR structuring (9 new files, 6 modified)
- [x] Story Bank P1: Gym integration (relevantTags, pre-gym suggestion modal, post-gym analyst comparison)
- [x] Story Creation Flow: Chat-based story building with Mentor Ni
- [x] Mentor Ni Chat: Full chat system with user context, Gemma vLLM + Gemini fallback
- [ ] Story Bank UI polish: framework selector, voice input, file upload, filtering, TTS
- [ ] Story Bank RAG: pgvector embeddings for semantic story matching
- [x] Content Safety MVP: Gemini safety settings + content filter service + pipeline integration
- [x] Enhanced Evaluation MVP: 9 sub-scores + proficiency level (A1-C2) + UI bars
- [x] Real-World Audio Upload MVP: Transcription + evaluation + frontend /realworld page

## Active Deployment
- Modal: optimindss7 / speakmate-pipeline-v2
- LiveKit: speakmate-yu7nfde8.livekit.cloud

## Known Issues
- Safari MP4 transcoding artifacts
- Modal cold start ~30s

## Session Handoff Notes
<!-- Khi kết thúc session, ghi note cho agent tiếp theo ở đây -->
- [2026-04-12] Khởi tạo cấu trúc dự án SSoT theo Phase 0 MLOps proposal. Set up directory junction cho Claude logs thành công.
- [2026-04-12] (Antigravity): Fixed LiveKit connecting twice due to React StrictMode in `page.tsx`. Made Modal agent speak the first line from scenario instead of hardcoded text. Removed gradient UI effects due to lag.
- [2026-04-12] (Claude Code): Fixed 3 LiveKit agent bugs on Modal: (1) sys.exit(0) from typer/livekit-agents crashing container — monkey-patched sys.exit to no-op for code 0; (2) CUDA LD_LIBRARY_PATH cu12→cu13; (3) voice1.wav volume mount stale — redeployed. Also fixed STT double VAD, LLM system prompt self-identification, TTS float32→int16 overflow. Migrated to optimindss3 workspace. Agent running stably (~5GB RAM, no crashes).
- [2026-04-13] (Claude Code): Completed ManualBridgeAgent full pipeline (VAD→STT→LLM→TTS) confirmed working end-to-end. Critical bug fixed: livekit-python ≥0.12 wraps audio frames in AudioFrameEvent(.frame) instead of yielding AudioFrame directly — `async for event in audio_stream: frame = event.frame if hasattr(event, 'frame') else event`. Also fixed: rtc.AudioStream(track, sample_rate=16000, num_channels=1) for correct Silero VAD input format. Pipeline confirmed: sr=16000, VAD detects speech, Whisper transcribes Vietnamese, Gemini responds, Valtec TTS streams @ 24000Hz.
- [2026-04-13] (Claude Code): Completed ManualBridgeAgent Phase 2+3 in modal_pipeline.py. (1) Rewrote class to accept stt_model/tts_model/voice_ref/genai_client; (2) start() now does TTS warm-up to discover sample rate → creates AudioSource dynamically, then parses participant metadata for scenario/history/userName; (3) Implemented _process_user_turn() with full STT(librosa resample 48k→16k + Whisper)→LLM(Gemini)→data channel→TTS(Valtec stream 20ms frames) pipeline; (4) Removed dead duplicate closure block from _livekit_entrypoint (had broken `participant` NameError). Data channel format {type,speaker,line} matches useLiveKitRoom.ts. NEEDS DEPLOY: modal deploy modal_pipeline.py on optimindss3.
- [2026-04-13] (Claude Code): Frontend conversation room redesign complete. Created 4 new components under frontend/src/components/practice/: RobotAvatar.tsx (breathing/glow Framer Motion, speaking badge), WebcamPreview.tsx (getUserMedia PiP top-left, auto-hides if no camera), FloatingTranscripts.tsx (AnimatePresence popLayout push-up text, CSS mask-image gradient fade, dims 0.2 opacity when user speaking), WaveformVisualizer.tsx (35 bars teal→purple gradient, 100ms interval, AnimatePresence). Redesigned page.tsx: full-screen dark bg, absolute header, centered RobotAvatar, waveform below avatar, floating transcripts above bottom bar, bottom controls with redo/mic/end. All original logic (LiveKit, HTTP fallback, hints, redo) preserved intact.
- [2026-04-13] (Antigravity): LiveKit agent pre-loading — pre-create LiveKit session on confirm page so agent starts loading while user reviews scenario. Added loading overlay on conversation page ("Đang chuẩn bị phòng luyện tập...") that blocks interaction until agent sends `agent_ready` via data channel. Mic auto-enables only after agent ready. Files: ScenarioContext.tsx (+livekitSession state), confirm/page.tsx (+pre-create), conversation/page.tsx (+overlay+gate), useLiveKitRoom.ts (+isAgentReady), modal_pipeline.py (+agent_ready signal). NEEDS DEPLOY: `modal deploy modal_pipeline.py` on optimindss3.
- [2026-04-14] (Claude Code): Reverted modal_pipeline.py + useLiveKitRoom.ts to checkpoint 3aeda72 (ZeroShotTTS + voice1.wav). STT/TTS improvement attempt (built-in TTS, RMS norm, lower VAD threshold) broke room entry — agent loaded fine but never received LiveKit jobs. Root cause unknown. Reverted via `git checkout 3aeda72 -- modal_pipeline.py frontend/src/hooks/useLiveKitRoom.ts`. Deployed to ptimindss5. Working state restored.
- [2026-04-14] (Claude Code): Fixed room entry blocked: (1) modal_pipeline.py start() never sent agent_ready signal — added publish_data after participant joins. (2) useLiveKitRoom.ts reverted hook lacked isAgentReady state entirely — page.tsx used undefined, overlay never cleared. Added isAgentReady state + agent_ready handler to hook. Pipeline confirmed working end-to-end.
- [2026-04-14] (Claude Code): STT improvements in modal_pipeline.py: (1) Added RMS normalization before Whisper (target -20dBFS, cap 20dB gain) to boost quiet speech. (2) Tuned Whisper params: no_speech_threshold=0.5, compression_ratio_threshold=2.4, condition_on_previous_text=False, temperature=0.0 — fixes hallucination "Cảm ơn các bạn đã theo dõi". (3) Switched STT model from `large-v3` → `large-v3-turbo` → `kiendt/PhoWhisper-large-ct2` (VinAI PhoWhisper, SOTA Vietnamese, CTranslate2 format). Note: `quocphu/PhoWhisper-ct2-FasterWhisper` failed — model.bin not at root (subdirectories). VAD params revert to `silero.VAD.load()` default — custom params incompatible with installed livekit-plugins-silero version. Deployed to ptimindss5. PENDING TEST.
- [2026-04-14] (Claude Code): Architecture v2 plan completed qua 3 vòng phản biện. Saved to `.ai/plans/architecture-v2.md`. Key decisions: (1) Control Plane (Node.js) vs Data Plane (Python) separation. (2) Python gọi Internal API backend thay vì Supabase trực tiếp. (3) JWT metadata chỉ chứa session_id, worker fetch context qua HTTP. (4) Optimistic UI: pending/confirmed turns. (5) Barge-in: cancel TTS task + interrupted flag. (6) Sliding window context (6 turns + summary). (7) Rate limit + authRequired trước LiveKit token. (8) Turn sequence numbers phát hiện data channel drop.
- [2026-04-14] (Claude Code): PhoWhisper (kiendt/PhoWhisper-large-ct2) deployed thành công trên ptimindss5. Model load OK trong Modal logs. STT cho tiếng Việt cải thiện. RMS normalization + anti-hallucination params đang active.
- [2026-04-14] (Claude Code): Architecture v2 Bước 1 hoàn tất — npm workspaces + shared contracts. Tạo root package.json (workspaces), packages/contracts/ (@speakmate/contracts với practice.ts + livekit.ts). Backend + frontend re-export từ shared package. Build cả 3 thành công (0 errors). Lưu ý: backend dùng zod v3, frontend dùng zod v4 — contracts package dùng plain TypeScript interfaces để tránh conflict.
- [2026-04-14] (Claude Code): Architecture v2 Bước 2 hoàn tất (code side). Modal pipeline giờ fetch context từ Internal API thay vì đọc metadata. Các thay đổi: (1) httpx added to pip_install; (2) backend-internal-key Modal secret added to LiveKitAgentWorker; (3) ManualBridgeAgent.start() checks meta.v >= 2 → fetches /api/internal/sessions/:id/context; (4) _build_system_prompt() fallback method added; (5) _process_user_turn() dùng self.system_prompt thay vì rebuild mỗi turn; (6) INTERNAL_API_KEY generated + set in backend/.env. PENDING: User cần tạo Modal secret "backend-internal-key" với INTERNAL_API_KEY + BACKEND_URL, rồi modal deploy. Xem hướng dẫn deploy bên dưới.
- [2026-04-14] (Claude Code): Deploy lên Modal reverted — dùng Cloudflare Tunnel thay thế. Backend accessible qua quick tunnel URL. Bước 2 verified: `[Bridge] Context fetched: 0 turns` confirms Internal API flow working. Fixed practice_sessions mode_check: 'livekit' → 'safe'.
- [2026-04-14] (Claude Code): Bước 3 — Turn persistence qua Internal API. Thêm vào modal_pipeline.py: (1) self.session_id + self.turn_counter trong ManualBridgeAgent; (2) _persist_turn() async method POST /api/internal/turns; (3) _process_user_turn() gửi turn_index trong data channel + asyncio.create_task persist cả User và AI turns. Backend POST /turns endpoint đã có sẵn từ Bước 2. Deployed.
- [2026-04-14] (Claude Code): Fixed context fetch ReadTimeout — increased httpx timeout from 10s→30s for context fetch and 10s→20s for turn persist. Cloudflare tunnel latency caused original 10s timeout to fail. Deployed to ptimindss5.
- [2026-04-15] (Claude Code): Bước 6 — Tách modal_pipeline.py thành ai/ folder structure. Tạo: ai/modal/image.py, ai/modal/app.py, ai/modal/http_pipeline.py, ai/modal/livekit_worker.py, ai/agents/bridge_agent.py. Di chuyển livekit_plugins/ → ai/livekit_plugins/. modal_pipeline.py giờ là thin entry point (10 dòng). Deploy thành công ptimindss5.
- [2026-04-14] (Claude Code): Bước 4 skipped (giữ HTTP fallback). Bước 5 — Dọn root hoàn tất: (1) test_*.py/js → tests/; (2) upload_*.py, fix_*.py, *.ps1 scripts → scripts/; (3) sample_voice/ copied → data/voice_samples/; (4) .gitignore thêm: *.gguf, *.exe, *.wav, *.log, __pycache__/, venv/, models/, F5-tts/, MiAI_VoiceClone_VoxCPM/, dia2/, valtec-tts-repo/, backend-stt/, backend-tts/, sample_voice/, vad_segments/, Modelfile.*, .cursorrules, .kilorules, GEMINI.md, config.json. Backend + frontend build verified OK.
- [2026-04-15] (Antigravity): Architecture v2 fully completed! (1) Security: Rate limiting `/livekit-session` (5/min). (2) Maintainability: Prompt externalization to `data/prompts/` with RAM caching in `PromptService`. (3) Performance: Sliding window context (6 turns) + background summarization in `bridge_agent.py`. (4) UX: Robust barge-in with accurate context truncation (char-based) and buffer flushing. (5) Optimistic UI: Frontend handles pending/confirmed transcripts via data channel signaling (`turn_confirmed`). (6) DB Migration: Added `interrupted` and `delivered_chars` to `conversation_turns`. Needs redeploy: `modal deploy modal_pipeline.py`.
- [2026-04-15] (Antigravity): Completed Quality Bugfixes: (1) **VAD**: Added 0.8s debounce timer in `bridge_agent.py`. (2) **STT**: Passed previous context to whisper via `initial_prompt` + tweaked temperature to `[0.0, 0.2]`. (3) **LLM Prompt**: Resticted constraints to heavily adhere to context. (4) **TTS Upgrade**: Swapped Valtec TTS to NeuTTS (`dinhthuan/neutts-air-vi`) loaded via `bfloat16`. Added `neucodec` / `phonemizer` processing in `neutts.py`.
- [2026-04-15] (Antigravity): Completed Phase 3 (Logic & Database) Gamification & Multi-stage Evaluation! 
  1. Updated `practice_sessions` table with parent versioning and created `realworld_challenges` DB table (`003_phase3_gamification.sql`). 
  2. Multi-stage Evaluation: Prompts reconfigured to enforce `Language`, `Content`, and `Emotion` JSON blocks. `EvaluationReport` data contracts added. 
  3. Mentor Ni Chatbot: Built `MentorAgent.ts` taking Scenario + Evaluation as context, outputting instructions/chat. Created `POST /mentor-chat`. 
  4. Gamification Loop: Built `BrainAgent.generateChallenge()` based on User Weaknesses, added full CRUD in Controller `generateChallenge`, `getUserChallenges`, `setChallengeDeadline`, `reportChallenge`. Verified clean build via `tsc`. Next Step: Run SQL migration and wire up UI for these new endpoints.
- [2026-04-15] (Antigravity): Completed Phase 3 Frontend Integration! Connected Multi-stage Evaluation Progress bars via `useSearchParams` + Data Fetching on `/evaluation/overall`. Created `MentorChatModal.tsx` and `ChallengeModal.tsx` to handle Coach messaging and generating Real-World prompts. Injected Gamification loop inside Dashboard (`page.tsx`) with `ActiveChallenges` component which allows users to directly record audio reports via HTML5 MediaRecorder. Frontend Builds OK. User MUST run SQL `003_phase3_gamification` to finalize schema before Testing.
- [2026-04-16] (Antigravity): Fix lỗi GPU Out-Of-Memory trên Modal và hallucination PhoWhisper. Pipeline chạy OK.
- [2026-04-17] (Antigravity): Debug & Optimization. Sửa lỗi TypeError: VAD.load() tại Modal do truyền sai tham số threshold. Đã xóa bỏ các tham số không tương thích và khởi động lại agent. Hệ thống đã sẵn sàng kết nối. Tinh chỉnh Dashboard: Nén nhỏ các component sidebar (ActiveChallenges, DailyTask, Streak, Calendar) và cố định panel phải (overflow-hidden) để hiển thị trọn vẹn mọi thông tin mà không cần cuộn.
- [2026-04-19] (Antigravity): Triển khai cơ chế Persistent Windows (Cửa sổ cố định) cho Backend và Frontend. 
  - Tạo `scripts/service_wrapper.ps1` để quản lý vòng lặp tiến trình trong terminal.
  - Cập nhật `scripts/start_speakmate.ps1` để tái sử dụng cửa sổ cũ qua cơ chế Signal Files (`$env:TEMP/SpeakMate_Signals`).
  - Cập nhật `scripts/stop_speakmate.ps1` để chỉ dừng Node.js mà không đóng cửa sổ terminal.
  - Người dùng có thể chạy `run-all.bat` và `stop-all.bat` mà không bị mở lại hàng loạt cửa sổ.
- [2026-04-19] (Claude Code): Di cư Modal workspace sang optimindss7. Tạo 3 secrets (gemini-api-key, livekit-keys, backend-internal-key với tunnel mới broadcasting-that-philips-broadway). Upload voice2.mp3 lên volume speakmate-valtec-models. Deploy thành công speakmate-pipeline-v2. Cập nhật backend/.env với URLs mới.
- [2026-04-19] (Claude Code): Gemini Live v3 pipeline fixes: (1) Added `NEXT_PUBLIC_USE_GEMINI_LIVE=true` to frontend/.env.local — was missing, causing confirm page to use classic pipeline. (2) Created `geminiLiveSessionSchema` validator (scenarioStr only, no conversationHistoryStr) — old shared schema blocked validation. (3) Made DB session creation non-blocking in `createGeminiLiveSession` — falls back to local ID if Supabase is down. **PENDING TEST**: Network was down during session — all external services (Supabase, LiveKit Cloud, Google) unreachable. Code changes are correct, need network to verify end-to-end.
- [2026-04-19] (Antigravity): Đáp ứng yêu cầu tạo thư mục độc lập để test mô hình PhoWhisper (`test_phowhisper`). Đã tạo `test_stt.py` với tuỳ chọn load `kiendt/PhoWhisper-large-ct2` (chạy trên CTranslate2 với fallback VAD tích hợp của faster-whisper), `requirements.txt` và `README.md` bao gồm hướng dẫn test độc lập mà không ảnh hưởng tới codebase lõi của dự án SpeakMate. Mọi tiến trình đã hoàn tất và test script chạy an toàn không load app overhead.
- [2026-04-19] (Antigravity): Lập kế hoạch test end-to-end cho LiveKit Pipeline (`livekit_test_plan.md`) bao gồm kiểm thử VAD, STT, NeuTTS, độ trễ và Context Retention. Kế hoạch không tự động thực thi theo yêu cầu của user.
- [2026-04-19] (Antigravity): Khắc phục lỗi 500 do `languageCodes` không còn được hỗ trợ trong v1alpha API của Gemini, giúp restore tính năng Gemini Direct (Live v3).
- [2026-04-19] (Antigravity): Cải tiến thiết kế trang Conversation Studio sang dạng Split-Screen. Mở khoá giới hạn không gian Chat, thay đổi luồng Transcript sang bên phải (chiếm 60% màn hình) để người dùng đọc được các đoạn hội thoại dài lớn và rõ ràng hơn.
- [2026-04-19] (Claude Code): Gemini Direct pipeline (v4) — browser kết nối trực tiếp tới Gemini Live API qua WebSocket, bypass hoàn toàn LiveKit. Files mới: `frontend/src/hooks/useGeminiDirect.ts` (core hook: AudioWorklet PCM16 capture → Gemini WebSocket → AudioContext playback), `frontend/public/pcm-capture-processor.js` (AudioWorklet). Backend: `createGeminiDirectToken` endpoint tạo ephemeral token qua `@google/genai` SDK. Frontend: tích hợp mode thứ 3 vào conversation page, ScenarioContext, confirm page. Feature flag: `NEXT_PUBLIC_USE_GEMINI_DIRECT=true`. Đã test ephemeral token endpoint OK. Cần test end-to-end trên browser.
- [2026-04-19] (Claude Code): Di cư Modal sang optimindss8 (optimindss7 hết billing). Deploy bao gồm: Gemini Flash STT thay PhoWhisper, debug audio saving vào `/debug_audio` volume, TTS retry 3x, pre-roll buffer. Thêm debug audio endpoint backend (`POST /debug-audio`) + frontend hook lưu WAV mỗi turn. Cập nhật stop_speakmate.ps1 kill theo port thay vì process name.
- [2026-04-20] (Claude Code): Story Bank P0+P1 hoàn tất. P0: Migration 004 (story_entries + story_practice_history tables), StoryBankAgent (Gemini STAR structuring), StoryBankController (CRUD), 6 REST endpoints (/api/storybank/*), Zod validators, 3 frontend pages (/stories, /stories/create, /stories/[id]). P1: BrainAgent relevantTags, useStoryBankSuggestions hook (client-side tag matching với diacritics normalization), StoryBankSuggestionModal (pre-gym), AnalystAgent.compareWithStoryBank (post-gym), coverage score section trong evaluation page. Fix auth: getAuthHeaders() cookie parsing cho @supabase/ssr@0.10. Fix Google OAuth: provider chưa được bật trên Supabase — user đã fix. Tiếp theo: UI polish (framework selector, voice input, file upload, filtering).
- [2026-04-21] (Claude Code): Story Creation Flow hoàn tất (P0 acceptance criteria 1-6). Thay flow cũ "2 câu hỏi follow-up" bằng chat tự do với Mentor Ni. Backend: thêm MentorAgent.chatForStory() (Gemini 2.0 Flash, multi-turn, JSON output {chatMessage, fieldTargeted}), POST /storybank/chat endpoint, PromptService.getStoryChatSystemPrompt() với framework-aware substitution, mở rộng StoryBankAgent.structureStory() nhận chatHistory param, thêm missingFields/completenessNote vào StoryStructureResponse contract + storybank.txt prompt. Frontend: tạo useStoryChat hook (localStorage persistence, session resume, structureNow), rewrite /stories/create page 3 stages (input→chatting→preview), inline chat UI với Ni avatar + fieldTargeted badges, missingFields banner vàng, "Quay lại chat" button. Không cần DB migration. Build OK cả backend + frontend. CẦN TEST: chạy backend+frontend, test chat endpoint, test full flow end-to-end.
- [2026-04-21] (Claude Code): Mentor Ni Chat full implementation. (1) Modal: gemma_worker.py (vLLM Gemma inference endpoint), gemma_image trong image.py. (2) DB: migration 005 (mentor_chat_sessions + mentor_chat_messages tables). (3) Backend: MentorContextService (user data aggregation — stats/sessions/stories/challenges parallel fetch, tag-based story matching), GemmaService (HTTP client cho vLLM với Gemini fallback), MentorAgent.generalChat() (JSON output {reply, intent, actionTaken, dataCards}), MentorChatController (POST /send + GET /history), mentor_chat.txt prompt. (4) Frontend: apiClient methods, useMentorChat hook rewrite (mock→real API, greeting mới mỗi lần mở, Ni nhớ context cũ qua server history). Build OK. CẦN: Chạy SQL migration 005, deploy Gemma worker lên Modal, test endpoint.
- [2026-04-22] (Gemini): Cập nhật UI Dashboard (`frontend/src/app/page.tsx`): Căn giữa thư mục bối cảnh "Bối cảnh đang luyện tập" (`RecentScenario`) vào chính giữa khoảng trống bên phải nút AvatarGreeting bằng cách dùng thẻ bọc `flex-1 flex justify-center`.
- [2026-04-22] (Gemini): Khắc phục lỗi Gemini SDK (`Cannot use 'in' operator to search for 'text' in [String]`) bằng cách chuẩn hoá toàn bộ payload `contents: string` thành định dạng chuẩn mảng đối tượng `TextPart` `[{ role: 'user', parts: [{ text: ... }] }]` trên TẤT CẢ các agent (Mentor, Brain, Analyst, StoryBank). Thay thế toàn bộ endpoint Gemma bằng Gemini 2.5 Flash API trong `MentorAgent.generalChat()`. Backend compile OK. Worker Gemma trên Modal hiện không còn bắt buộc chạy.
- [2026-04-22] (Claude Code): API logic audit & fix. (1) Evaluation page: wired real `apiClient.evaluateSession()` thay vì mock data — hiện gọi Gemini API thực, fallback mock nếu lỗi. UI chuyển sang multi-stage format (language/content/emotion progress bars). (2) Evaluation prompt: sửa score scale 0-10 → 0-100 cho khớp frontend. (3) `saveEvaluation` DB: thêm `report_data` JSONB để lưu full EvaluationReport (overall page đọc từ field này). (4) Thêm 3 missing routes: `POST /challenge/feedback/voice`, `POST /challenge/feedback/form`, `POST /challenge/skip` + controller methods + `updateChallengeStatus` DB method. (5) Re-enable pre-loading trên confirm page (Gemini Direct/LiveKit/LiveKit token pre-created trước khi vào phòng). Build OK cả backend + frontend. LƯU Ý: DB cần có column `report_data` (JSONB) trong bảng `evaluations` — nếu chưa có, cần thêm migration.
- [2026-04-22] (Claude Code): Fix Mentor Ni Chat. Nguyên nhân: (1) `mentor-chat.routes.ts` dùng `authRequired` → guest 401. Sửa thành `authOptional`. (2) DB tables `mentor_chat_sessions`/`mentor_chat_messages` (migration 005) chưa chạy → controller crash khi query. Sửa: wrap tất cả DB calls trong try/catch non-blocking — chat works without DB. (3) Tăng Gemini SDK timeout 15s → 30s (Windows DNS connect timeout). Guest users giờ có thể chat với Ni (không lưu history). Authenticated users vẫn lưu full history nếu DB tables tồn tại. Tested: curl POST /mentor-chat/send → response 200 với JSON hợp lệ.
- [2026-04-22] (Claude Code): 3 Improvements MVP hoàn tất. (A) Content Safety: Gemini safety settings trên tất cả agents (brain/analyst/mentor/bridge), ContentFilterService regex blocklist (VI/EN profanity + PII detection + harmful topics), tích hợp vào practice.controller + bridge_agent.py. (B) Enhanced Evaluation: evaluation.txt thêm 9 sub-scores (vocabulary/grammar/honorific, persuasion/clarity/professionalism, empathy/confidence/toneControl) + proficiencyLevel CEFR A1-C2. Contracts StageFeedback thêm subScores, EvaluationReport thêm proficiencyLevel. AnalystAgent.validateReport() default missing sub-scores. Frontend overall page hiện sub-score bars + proficiency badge. (C) Real-World Audio: TranscriptionService (Gemini inline_data), realworld_evaluation.txt prompt, POST /realworld/upload endpoint (transcode→transcribe→evaluate synchronous), frontend /realworld page (drag-drop upload + context input + results). Không cần DB migration. Build OK cả backend + frontend. CẦN TEST: xem hướng dẫn test thủ công bên dưới.
- [2026-04-22] (Gemini): Fixed VAD asset loading error in frontend. Copied missing `ort-wasm-simd-threaded.mjs` and related assets to `frontend/public`. Analyzed voice pipeline and documented improvement strategies in `pipeline_analysis.md`. Reduced `_vad_finalize` delay in `bridge_agent.py` from 3.0s to 1.0s to improve conversational flow and reduce user "wait time" which often leads to poor transcripts.
- [2026-04-23] (Claude Code): Dual-character TTS — NeuTTS HTTP endpoint trên Modal. Gemini TTS REST API không hoạt động (text-only / model not found). Giải pháp: thêm `TTSEndpoint` class vào `ai/modal/livekit_worker.py` — load NeuTTS trong `@modal.enter()`, expose `POST /tts` nhận `{text, voice_idx}` trả về WAV base64. voice_idx=0 → voice1.wav (char1), voice_idx=1 → voice2.mp3 (char2). Fallback: nếu voice1.wav chưa có trên volume, dùng voice2.mp3. Backend: `VoiceAgent.generateTTS(text, charIdx)` gọi Modal HTTP; `config.modalTtsUrl` từ env `MODAL_TTS_URL`. Frontend: `playPcmBase64` → `playAudioBase64` hỗ trợ cả WAV và raw PCM. Script: `upload_voice_to_modal.py` cập nhật upload cả voice1.wav + voice2.mp3. CẦN LÀM: (1) `python scripts/upload_voice_to_modal.py`, (2) `modal deploy modal_pipeline.py`, (3) copy TTSEndpoint URL → `backend/.env MODAL_TTS_URL=...`.
- [2026-04-23] (Claude Code): Story Bank STAR/CAR/PREP fix: (1) storybank.agent.ts — force needsFollowUp=false khi chatHistory/followUpAnswers có data; pass framework param xuống agent; defaults theo từng framework (challenge/action/result cho CAR, point/reason/example/point2 cho PREP). (2) story_chat.txt — Ni PHẢI hỏi TẤT CẢ fields theo thứ tự, không skip. (3) storybank.txt — hỗ trợ cả 3 frameworks, đọc framework từ input. (4) Evaluation metrics fix: apiClient.evaluateSession() giờ trả về {evaluationReport, previousMetrics}; backend tính avgResponseTime synchronously + attach vào sessionMetrics; thêm GET /practice/previous-metrics endpoint; seedDemoData cache previousMetrics vào localStorage. Frontend dùng previousMetrics để hiện mũi tên so sánh. Guest fallback: dùng speakmate_previous_metrics từ localStorage.
- [2026-04-23] (Claude Code): Dual-character practice room redesign hoàn tất. Thay Gemini Direct WebSocket (1 voice) bằng HTTP round-trip per turn với relevance scoring. Files thay đổi: (1) `data/prompts/character_score.txt` (mới) — prompt JSON cho mỗi nhân vật tự chấm điểm 0-100; (2) `PromptService.buildCharacterScoringPrompt()` (mới); (3) `VoiceAgent.interactDualCharacter()` (mới) — STT→2 Gemini parallel scoring calls→pick winner→Gemini TTS với voice riêng (Kore/Puck/Aoede/Charon); (4) `POST /practice/interact-dual` route + controller; (5) `apiClient.interactDualChar()` frontend; (6) `conversation/page.tsx` — khi scenario có ≥2 characters, tự động bypass Gemini Direct → dùng HTTP + `playPcmBase64()`; (7) `useGeminiDirect.ts` — xóa dead code `muteGeminiAudio` + `outputTranscription` block, fix fallback counter dùng `aiTurnCounter` riêng; (8) `conversation_multi.txt` — bỏ strict alternation rule, thay bằng relevance-based selection. Backend + frontend compile OK. CẦN TEST: scenario 2 người → nói thuộc domain char1 → char1 phải trả lời với giọng Kore/Puck; nói về char2 → char2 trả lời với giọng Aoede/Charon. CẢNH BÁO: Gemini TTS model `gemini-2.5-flash-preview-tts` cần v1alpha API — nếu lỗi TTS sẽ fallback về text-only (browser TTS).
