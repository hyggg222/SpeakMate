# Data Models - SpeakMate Core

## Overview
This document describes the database schema used by the SpeakMate core application, implemented via Drizzle ORM.

## Tables

### 1. `post` (Template Example)
- **Description:** A placeholder table from the T3 starter template.
- **Columns:**
  - `id`: `integer`, Primary Key, Identity.
  - `name`: `varchar(256)`.
  - `createdAt`: `timestamp`, Default now.
  - `updatedAt`: `timestamp`, Updated on change.
- **Indexes:**
  - `name_idx` on column `name`.

## Note
The project is currently using a `pgTableCreator` prefix: `_temp_init_`. Actual application-specific tables (users, sessions, audio_logs) are planned in the architecture but not yet implemented in the schema file.

---
*Generated by SpeakMate Document Project Workflow on 2026-01-31*